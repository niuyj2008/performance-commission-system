# 🚀 Render 部署快速开始

## 5 分钟部署到云端

### 前提条件
- ✅ GitHub 账号
- ✅ Render 账号（免费，访问 https://render.com 注册）

### 步骤 1: 推送代码到 GitHub（2 分钟）

```bash
# 运行部署脚本
./deploy-to-render.sh
```

如果脚本提示需要配置远程仓库：

1. 在 GitHub 创建新仓库：https://github.com/new
   - 仓库名：`performance-commission-system`
   - 设为私有（Private）
   - 不要初始化 README

2. 添加远程仓库并推送：
```bash
git remote add origin https://github.com/你的用户名/performance-commission-system.git
git branch -M main
git push -u origin main
```

### 步骤 2: 在 Render 部署（3 分钟）

1. **访问 Render**
   - 打开 https://dashboard.render.com
   - 使用 GitHub 账号登录

2. **创建新服务**
   - 点击 "New +" 按钮
   - 选择 "Blueprint"
   - 授权 Render 访问你的 GitHub
   - 选择 `performance-commission-system` 仓库
   - 点击 "Connect"

3. **应用配置**
   - Render 会自动检测 `render.yaml`
   - 检查配置无误后，点击 "Apply"

4. **等待部署**
   - 部署进度会实时显示
   - 大约 3-5 分钟完成
   - 看到 "Live" 状态表示成功

### 步骤 3: 访问应用

部署成功后，你会得到一个 URL：
```
https://performance-commission-system.onrender.com
```

点击 URL 即可访问系统！

## 默认登录信息

查看 `server/db/seed.js` 文件了解默认账号。

通常是：
- 管理员账号：admin
- 密码：查看 seed.js 文件

## 视频教程

### 推送到 GitHub
```bash
# 1. 初始化（如果需要）
git init

# 2. 添加文件
git add .

# 3. 提交
git commit -m "Deploy to Render"

# 4. 添加远程仓库
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 5. 推送
git push -u origin main
```

### Render 部署流程
```
登录 Render → New + → Blueprint → 选择仓库 → Apply → 等待部署
```

## 常见问题

### ❓ 首次访问很慢？
免费套餐会休眠，首次访问需要 30-60 秒启动。之后访问会很快。

### ❓ 如何保持应用不休眠？
使用 UptimeRobot（免费）：
1. 访问 https://uptimerobot.com
2. 添加监控：`https://你的应用.onrender.com/api/health`
3. 间隔设为 5 分钟

### ❓ 数据会丢失吗？
不会！已配置持久化存储，数据永久保存。

### ❓ 如何更新应用？
```bash
git add .
git commit -m "Update"
git push origin main
```
Render 会自动重新部署。

## 下一步

✅ 部署成功后：
1. 登录系统
2. 修改默认密码
3. 导入员工数据
4. 导入项目数据
5. 开始使用

## 需要帮助？

- 📖 详细指南：查看 `RENDER_DEPLOYMENT_GUIDE.md`
- ✅ 检查清单：查看 `RENDER_CHECKLIST.md`
- 💬 Render 支持：https://render.com/docs

---

**预计总时间**: 5-10 分钟
**难度**: ⭐⭐☆☆☆（简单）
**费用**: 完全免费

🎉 开始部署吧！
