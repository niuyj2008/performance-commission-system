# 系统打包发布说明

## 📦 打包清单

### 必须包含的文件和文件夹

#### 核心文件
```
绩效提成管理系统/
├── server/                 # 后端代码（必须）
├── public/                 # 前端页面（必须）
├── data/                   # 数据库目录（必须，可为空）
├── package.json            # 依赖配置（必须）
├── package-lock.json       # 依赖锁定（必须）
├── Dockerfile              # Docker镜像（必须）
├── docker-compose.yml      # Docker编排（必须）
└── .dockerignore           # Docker忽略（必须）
```

#### Windows启动文件
```
├── 启动系统.bat            # Windows一键启动
├── 停止系统.bat            # Windows停止服务
├── 查看日志.bat            # Windows查看日志
└── 备份数据.bat            # Windows备份数据
```

#### Mac/Linux启动文件
```
├── 启动系统.sh             # Mac/Linux一键启动
├── 停止系统.sh             # Mac/Linux停止服务
└── 云服务器部署.sh         # 云服务器自动部署
```

#### 文档文件
```
├── README_部署.md          # 快速开始（必读）
├── 部署指南.md             # 详细部署文档
├── 部署检查清单.md         # 验证清单
├── DEPLOYMENT_SUMMARY.md   # 部署方案总结
└── 打包发布说明.md         # 本文档
```

### 不要包含的文件

```
❌ node_modules/           # 依赖包（太大，会自动安装）
❌ .git/                   # Git仓库
❌ .kiro/                  # 开发工具配置
❌ tests/                  # 测试文件
❌ *.md（除了上述文档）     # 其他开发文档
❌ analyze_*.py            # 分析脚本
❌ import-*.js             # 导入脚本
❌ test-*.js               # 测试脚本
❌ update-*.js             # 更新脚本
❌ 2025上半年/             # 示例数据
❌ 项目/                   # 示例数据
❌ *.rar                   # 压缩文件
❌ .DS_Store               # Mac系统文件
```

---

## 📋 打包步骤

### 方法一: 手动打包（推荐）

#### 1. 创建发布文件夹
```bash
mkdir 绩效提成管理系统_v1.0
cd 绩效提成管理系统_v1.0
```

#### 2. 复制必要文件
```bash
# 复制核心文件夹
cp -r server public data ../绩效提成管理系统_v1.0/

# 复制配置文件
cp package.json package-lock.json ../绩效提成管理系统_v1.0/
cp Dockerfile docker-compose.yml .dockerignore ../绩效提成管理系统_v1.0/

# 复制启动脚本
cp *.bat *.sh ../绩效提成管理系统_v1.0/

# 复制文档
cp README_部署.md 部署指南.md 部署检查清单.md ../绩效提成管理系统_v1.0/
cp DEPLOYMENT_SUMMARY.md 打包发布说明.md ../绩效提成管理系统_v1.0/
```

#### 3. 清理data文件夹
```bash
# 删除现有数据库（用户会自动初始化）
rm -f data/database.sqlite

# 或者保留示例数据（如果需要）
# 保持data文件夹存在但为空
```

#### 4. 压缩打包
```bash
# Windows: 使用7-Zip或WinRAR
# 右键 -> 添加到压缩文件 -> 绩效提成管理系统_v1.0.zip

# Mac/Linux
zip -r 绩效提成管理系统_v1.0.zip 绩效提成管理系统_v1.0/
# 或
tar -czf 绩效提成管理系统_v1.0.tar.gz 绩效提成管理系统_v1.0/
```

---

### 方法二: 使用脚本打包

创建 `打包脚本.sh`:

```bash
#!/bin/bash

VERSION="1.0"
PACKAGE_NAME="绩效提成管理系统_v${VERSION}"

echo "开始打包 ${PACKAGE_NAME}..."

# 创建临时目录
mkdir -p ${PACKAGE_NAME}

# 复制文件
echo "复制核心文件..."
cp -r server public data ${PACKAGE_NAME}/
cp package.json package-lock.json ${PACKAGE_NAME}/
cp Dockerfile docker-compose.yml .dockerignore ${PACKAGE_NAME}/

echo "复制启动脚本..."
cp *.bat *.sh ${PACKAGE_NAME}/

echo "复制文档..."
cp README_部署.md 部署指南.md 部署检查清单.md ${PACKAGE_NAME}/
cp DEPLOYMENT_SUMMARY.md 打包发布说明.md ${PACKAGE_NAME}/

# 清理数据库
echo "清理数据库..."
rm -f ${PACKAGE_NAME}/data/database.sqlite

# 创建README
cat > ${PACKAGE_NAME}/开始使用.txt << 'EOF'
绩效提成管理系统 - 快速开始

1. 安装Docker Desktop
   Windows: https://www.docker.com/products/docker-desktop
   Mac: https://www.docker.com/products/docker-desktop

2. 启动系统
   Windows: 双击"启动系统.bat"
   Mac/Linux: 运行 ./启动系统.sh

3. 访问系统
   浏览器打开: http://localhost:3000

4. 登录
   用户名: admin
   密码: admin123

详细说明请查看"README_部署.md"
EOF

# 压缩
echo "压缩打包..."
zip -r ${PACKAGE_NAME}.zip ${PACKAGE_NAME}/

# 清理临时目录
rm -rf ${PACKAGE_NAME}

echo "打包完成: ${PACKAGE_NAME}.zip"
```

---

## 📤 发布准备

### 1. 版本信息
在`package.json`中更新版本号:
```json
{
  "version": "1.0.0",
  "description": "建筑设计公司绩效提成分配管理系统"
}
```

### 2. 创建发布说明
创建`RELEASE_NOTES.md`:
```markdown
# 版本 1.0.0 发布说明

发布日期: 2024年12月16日

## 新功能
- ✅ 项目管理
- ✅ 员工管理
- ✅ 提成计算
- ✅ 发放管理
- ✅ 统计报表
- ✅ 系统配置

## 部署方式
- Docker一键部署
- 云服务器部署
- Windows服务器部署

## 系统要求
- CPU: 2核或以上
- 内存: 2GB或以上
- 硬盘: 20GB可用空间

## 默认账号
- 用户名: admin
- 密码: admin123

## 重要提示
⚠️ 首次登录后请立即修改密码！
```

### 3. 测试打包文件
1. 解压打包文件到新目录
2. 按照文档步骤部署
3. 验证所有功能正常
4. 确认文档准确无误

---

## 📊 文件大小估算

| 内容 | 大小 |
|------|------|
| server/ | ~500KB |
| public/ | ~200KB |
| data/ (空) | ~1KB |
| 配置文件 | ~10KB |
| 脚本文件 | ~20KB |
| 文档文件 | ~100KB |
| **总计** | **~1MB** |

压缩后约: **300-500KB**

---

## 🚀 发布渠道

### 内部发布
1. 公司内网文件服务器
2. 企业网盘（如企业微信、钉钉）
3. 内部Git仓库

### 外部发布（如需要）
1. GitHub Releases
2. 云存储（阿里云OSS、腾讯云COS）
3. 百度网盘、OneDrive

---

## 📝 发布检查清单

### 打包前
- [ ] 代码已测试通过
- [ ] 所有功能正常工作
- [ ] 文档已更新
- [ ] 版本号已更新
- [ ] 默认数据已准备

### 打包中
- [ ] 所有必要文件已包含
- [ ] 不必要文件已排除
- [ ] 文件结构正确
- [ ] 脚本权限正确

### 打包后
- [ ] 压缩包大小合理（<10MB）
- [ ] 解压测试通过
- [ ] 部署测试通过
- [ ] 文档测试通过
- [ ] 跨平台测试通过

---

## 🎯 交付物清单

### 给客户的文件
```
绩效提成管理系统_v1.0.zip
├── 开始使用.txt           # 快速开始（必读）
├── README_部署.md          # 详细部署指南
├── 部署检查清单.md         # 验证清单
├── 启动系统.bat            # Windows启动
├── 启动系统.sh             # Mac/Linux启动
├── 停止系统.bat            # Windows停止
├── 停止系统.sh             # Mac/Linux停止
├── 查看日志.bat            # 查看日志
├── 备份数据.bat            # 备份数据
├── 云服务器部署.sh         # 云服务器部署
└── [系统文件...]
```

### 给技术人员的文件
```
绩效提成管理系统_技术文档.zip
├── DEPLOYMENT_SUMMARY.md   # 部署方案总结
├── 部署指南.md             # 详细技术文档
├── 打包发布说明.md         # 本文档
├── FINAL_SYSTEM_SUMMARY.md # 系统功能总结
└── [其他技术文档...]
```

---

## 💡 使用建议

### 对于非技术用户
1. 只需要`绩效提成管理系统_v1.0.zip`
2. 解压后阅读`开始使用.txt`
3. 按照步骤操作即可

### 对于技术人员
1. 同时提供两个压缩包
2. 先阅读技术文档
3. 了解系统架构后再部署

---

## 🔄 版本管理

### 版本号规则
- 主版本号.次版本号.修订号
- 例如: 1.0.0, 1.1.0, 1.1.1

### 版本说明
- **1.0.0**: 首次发布
- **1.1.0**: 新增功能
- **1.0.1**: Bug修复

### 更新策略
- 每月发布一次小版本
- 每季度发布一次大版本
- 紧急Bug立即发布修订版

---

## ✅ 最终检查

打包完成后,请确认:

1. ✅ 压缩包能正常解压
2. ✅ 文件结构完整
3. ✅ 文档清晰易懂
4. ✅ 脚本能正常运行
5. ✅ 系统能成功部署
6. ✅ 所有功能正常工作
7. ✅ 跨平台兼容性良好

---

**打包完成后,你就可以将系统交付给用户了!** 🎉

**祝部署顺利!** 🚀
