# 绩效提成管理系统 - 傻瓜式部署指南

## 📋 目录
1. [部署方式选择](#部署方式选择)
2. [方案一: Docker部署（推荐）](#方案一-docker部署推荐)
3. [方案二: 云服务器部署](#方案二-云服务器部署)
4. [方案三: 本地Windows服务器](#方案三-本地windows服务器)
5. [常见问题](#常见问题)

---

## 部署方式选择

### 🎯 推荐方案对比

| 方案 | 难度 | 成本 | 适用场景 | 推荐度 |
|------|------|------|----------|--------|
| Docker本地部署 | ⭐ | 免费 | 公司内网使用 | ⭐⭐⭐⭐⭐ |
| 阿里云/腾讯云 | ⭐⭐ | 约¥100/月 | 需要外网访问 | ⭐⭐⭐⭐ |
| Windows服务器 | ⭐⭐⭐ | 免费 | 有Windows服务器 | ⭐⭐⭐ |

---

## 方案一: Docker部署（推荐）⭐⭐⭐⭐⭐

### 为什么推荐Docker？
- ✅ 一键启动，无需配置环境
- ✅ 跨平台支持（Windows/Mac/Linux）
- ✅ 自动重启，稳定可靠
- ✅ 数据自动备份
- ✅ 完全免费

### 步骤1: 安装Docker

#### Windows系统
1. 访问 https://www.docker.com/products/docker-desktop
2. 下载 Docker Desktop for Windows
3. 双击安装包，一路"下一步"
4. 安装完成后重启电脑
5. 启动 Docker Desktop（桌面图标）

#### Mac系统
1. 访问 https://www.docker.com/products/docker-desktop
2. 下载 Docker Desktop for Mac
3. 拖动到应用程序文件夹
4. 启动 Docker Desktop

#### 验证安装
打开命令行（Windows: cmd / Mac: Terminal），输入:
```bash
docker --version
```
看到版本号就说明安装成功！

### 步骤2: 准备系统文件

1. 将整个项目文件夹复制到目标电脑
2. 确保以下文件存在:
   - `Dockerfile`
   - `docker-compose.yml`
   - `启动系统.bat` (Windows) 或 `启动系统.sh` (Mac/Linux)
   - `server/` 文件夹
   - `public/` 文件夹
   - `data/` 文件夹

### 步骤3: 一键启动

#### Windows系统
1. 双击 `启动系统.bat`
2. 等待1-2分钟
3. 浏览器自动打开系统页面
4. 完成！

#### Mac/Linux系统
1. 打开终端
2. 进入项目文件夹
3. 运行: `./启动系统.sh`
4. 等待1-2分钟
5. 访问 http://localhost:3000
6. 完成！

### 步骤4: 登录系统

默认管理员账号:
- **用户名**: admin
- **密码**: admin123

⚠️ **重要**: 首次登录后请立即修改密码！

### 日常使用

#### 启动系统
- Windows: 双击 `启动系统.bat`
- Mac/Linux: 运行 `./启动系统.sh`

#### 停止系统
- Windows: 双击 `停止系统.bat`
- Mac/Linux: 运行 `./停止系统.sh`

#### 查看日志
- Windows: 双击 `查看日志.bat`
- Mac/Linux: 运行 `docker-compose logs -f`

#### 备份数据
- Windows: 双击 `备份数据.bat`
- Mac/Linux: 手动复制 `data` 文件夹

---

## 方案二: 云服务器部署

### 适用场景
- 需要在公司外部访问系统
- 多个办公地点需要共享数据
- 需要更高的可靠性

### 推荐云服务商

#### 1. 阿里云（推荐）
- 网址: https://www.aliyun.com
- 配置: 2核2G，40G硬盘
- 费用: 约¥100/月
- 优点: 国内访问快，服务稳定

#### 2. 腾讯云
- 网址: https://cloud.tencent.com
- 配置: 2核2G，40G硬盘
- 费用: 约¥100/月
- 优点: 新用户优惠多

### 部署步骤（以阿里云为例）

#### 步骤1: 购买服务器
1. 注册阿里云账号
2. 进入"云服务器ECS"
3. 选择配置:
   - 地域: 选择离公司最近的
   - 实例规格: 2核2G
   - 镜像: Ubuntu 22.04
   - 网络: 默认
   - 安全组: 开放3000端口
4. 购买并等待创建完成

#### 步骤2: 连接服务器
1. 在阿里云控制台找到服务器IP地址
2. 使用SSH工具连接（推荐使用 FinalShell）
   - 下载: http://www.hostbuf.com/
   - 输入服务器IP、用户名(root)、密码

#### 步骤3: 安装Docker
连接到服务器后，依次执行:

```bash
# 更新系统
sudo apt update

# 安装Docker
curl -fsSL https://get.docker.com | sh

# 启动Docker
sudo systemctl start docker
sudo systemctl enable docker

# 安装Docker Compose
sudo apt install docker-compose -y
```

#### 步骤4: 上传系统文件
1. 使用FinalShell的文件传输功能
2. 将整个项目文件夹上传到服务器的 `/opt/commission-system/`

#### 步骤5: 启动系统
```bash
cd /opt/commission-system
sudo docker-compose up -d
```

#### 步骤6: 配置域名（可选）
1. 购买域名（如 commission.yourcompany.com）
2. 在域名管理中添加A记录，指向服务器IP
3. 安装Nginx反向代理（可选，用于HTTPS）

### 访问系统
- 通过IP访问: http://服务器IP:3000
- 通过域名访问: http://你的域名.com:3000

---

## 方案三: 本地Windows服务器

### 适用场景
- 公司有专门的Windows服务器
- 不想使用Docker
- 需要完全控制

### 前置要求
- Windows Server 2016 或更高版本
- 或 Windows 10/11 专业版

### 步骤1: 安装Node.js
1. 访问 https://nodejs.org
2. 下载LTS版本（推荐18.x）
3. 双击安装，一路"下一步"
4. 验证安装:
   ```cmd
   node --version
   npm --version
   ```

### 步骤2: 准备系统文件
1. 将项目文件夹复制到服务器（如 `C:\commission-system\`）
2. 打开命令提示符（管理员权限）
3. 进入项目目录:
   ```cmd
   cd C:\commission-system
   ```

### 步骤3: 安装依赖
```cmd
npm install --production
```

### 步骤4: 初始化数据库
```cmd
node server/db/seed.js
```

### 步骤5: 创建Windows服务

#### 方法1: 使用PM2（推荐）
```cmd
# 安装PM2
npm install -g pm2
npm install -g pm2-windows-service

# 安装Windows服务
pm2-service-install

# 启动应用
pm2 start server/index.js --name commission-system
pm2 save
```

#### 方法2: 使用NSSM
1. 下载NSSM: https://nssm.cc/download
2. 解压到 `C:\nssm\`
3. 打开命令提示符（管理员）:
   ```cmd
   cd C:\nssm\win64
   nssm install CommissionSystem
   ```
4. 在弹出窗口中配置:
   - Path: `C:\Program Files\nodejs\node.exe`
   - Startup directory: `C:\commission-system`
   - Arguments: `server/index.js`
5. 点击"Install service"

### 步骤6: 启动服务
```cmd
# PM2方式
pm2 start commission-system

# NSSM方式
net start CommissionSystem
```

### 步骤7: 配置防火墙
1. 打开"Windows Defender 防火墙"
2. 点击"高级设置"
3. 新建入站规则:
   - 规则类型: 端口
   - 协议: TCP
   - 端口: 3000
   - 操作: 允许连接
   - 名称: 绩效提成系统

### 访问系统
- 本机访问: http://localhost:3000
- 局域网访问: http://服务器IP:3000

---

## 常见问题

### Q1: Docker启动失败怎么办？
**A**: 
1. 确认Docker Desktop已启动
2. 检查3000端口是否被占用:
   ```cmd
   # Windows
   netstat -ano | findstr :3000
   
   # Mac/Linux
   lsof -i :3000
   ```
3. 查看错误日志: `docker-compose logs`

### Q2: 忘记管理员密码怎么办？
**A**: 
1. 停止系统
2. 删除 `data/database.sqlite` 文件
3. 重新启动系统（会重新初始化数据库）
4. 使用默认密码登录: admin/admin123

### Q3: 如何修改端口？
**A**: 
1. 编辑 `docker-compose.yml`
2. 修改 `ports` 部分:
   ```yaml
   ports:
     - "8080:3000"  # 改为8080端口
   ```
3. 重新启动系统

### Q4: 数据保存在哪里？
**A**: 
- 所有数据保存在 `data/database.sqlite` 文件中
- 定期备份这个文件即可

### Q5: 如何升级系统？
**A**: 
1. 备份 `data` 文件夹
2. 停止系统
3. 替换新的系统文件
4. 保留 `data` 文件夹
5. 重新启动系统

### Q6: 可以同时多人使用吗？
**A**: 
- 可以！系统支持多用户同时访问
- 建议同时在线用户不超过50人

### Q7: 需要连接互联网吗？
**A**: 
- 部署时需要联网下载依赖
- 运行时不需要互联网
- 局域网内即可正常使用

### Q8: 如何设置开机自动启动？
**A**: 

**Docker方式**:
- Docker Desktop设置中勾选"开机启动"
- 容器会自动启动（restart: unless-stopped）

**Windows服务方式**:
- 服务已设置为自动启动
- 无需额外配置

### Q9: 系统运行慢怎么办？
**A**: 
1. 检查服务器配置（建议2核2G以上）
2. 定期清理浏览器缓存
3. 检查数据库大小（超过100MB建议优化）
4. 重启系统

### Q10: 如何迁移到新服务器？
**A**: 
1. 在旧服务器备份 `data` 文件夹
2. 在新服务器部署系统
3. 停止新服务器的系统
4. 用备份的 `data` 文件夹替换新服务器的
5. 启动新服务器的系统

---

## 技术支持

### 联系方式
- 如遇到问题，请保存错误截图
- 记录操作步骤
- 联系技术支持

### 日志位置
- Docker方式: `docker-compose logs`
- Windows服务: `C:\commission-system\logs\`

### 系统要求

#### 最低配置
- CPU: 2核
- 内存: 2GB
- 硬盘: 20GB
- 网络: 局域网

#### 推荐配置
- CPU: 4核
- 内存: 4GB
- 硬盘: 40GB
- 网络: 100Mbps

---

## 安全建议

1. ✅ 首次登录后立即修改管理员密码
2. ✅ 定期备份数据（建议每周一次）
3. ✅ 不要将系统直接暴露在公网
4. ✅ 使用强密码（至少8位，包含字母数字符号）
5. ✅ 定期更新系统
6. ✅ 限制管理员账号数量
7. ✅ 定期检查用户权限

---

## 维护建议

### 每日
- 检查系统是否正常运行
- 查看是否有异常登录

### 每周
- 备份数据库文件
- 检查磁盘空间

### 每月
- 清理旧的备份文件
- 检查系统更新
- 审查用户权限

### 每季度
- 完整系统测试
- 性能优化
- 安全审计

---

## 附录: 快速命令参考

### Docker命令
```bash
# 启动系统
docker-compose up -d

# 停止系统
docker-compose down

# 查看日志
docker-compose logs -f

# 重启系统
docker-compose restart

# 查看状态
docker-compose ps

# 进入容器
docker exec -it commission-system sh
```

### 数据库命令
```bash
# 备份数据库
cp data/database.sqlite data/database.backup.sqlite

# 查看数据库
sqlite3 data/database.sqlite

# 导出数据
sqlite3 data/database.sqlite .dump > backup.sql
```

---

**祝您部署顺利！** 🎉
